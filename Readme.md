# How to reconfigure for another computer

Delete EDM_BACKUP/config.txt and double click the exe file

# How to create the exe file

run "pyinstaller --onefile edm_backup.py" in the directory of where edm_backup.py is
